# MineOS Environment Configuration Template
# Copy this file to .env and configure with your settings

# ============================================
# Database Configuration
# ============================================
# Options: sqlite
DB_TYPE=sqlite

# SQLite (default - simple single file database)
ConnectionStrings__DefaultConnection=Data Source=/app/data/mineos.db

# ============================================
# Authentication & Security
# ============================================
# Admin user credentials (created on first run)
Auth__SeedUsername=admin
Auth__SeedPassword=admin123!

# JWT configuration for authentication tokens
Auth__JwtSecret=change-this-to-a-random-secret-key-at-least-32-characters-long
Auth__JwtIssuer=mineos
Auth__JwtAudience=mineos
# Note: docker-compose reads Auth__JwtExpiryMinutes (default 60)
Auth__JwtExpiryMinutes=60

# API Key for programmatic access (generated automatically if not set)
ApiKey__SeedKey=

# ============================================
# Host Configuration
# ============================================
# Where Minecraft server files live on the host. Bind-mounted to
# /var/games/minecraft inside the container. A relative path is resolved against
# this repo; keep it relative unless you have a reason not to, since an absolute
# Linux path on Docker Desktop lands inside the VM rather than on your disk.
MINECRAFT_PATH=./minecraft

# Where the API keeps its SQLite database, bind-mounted to /app/data.
DATA_PATH=./data

# Older names for the two above. Still honoured, so existing .env files keep
# working; MINECRAFT_PATH / DATA_PATH win if both are set.
# HOST_BASE_DIRECTORY=./minecraft
# Data__Directory=./data

# Base directory for all Minecraft-related files
Host__BaseDirectory=/var/games/minecraft

# Directory structure (relative to BaseDirectory)
Host__ServersPathSegment=servers
Host__ProfilesPathSegment=profiles
Host__BackupsPathSegment=backups
Host__ArchivesPathSegment=archives
Host__ImportsPathSegment=imports

# File ownership (Linux UID/GID)
Host__OwnerUid=1000
Host__OwnerGid=1000

# Docker networking (bridge = isolated, host = required for LAN discovery)
MINEOS_NETWORK_MODE=bridge

# Build Docker images from source instead of pulling from registry
# MINEOS_BUILD_FROM_SOURCE=false

# Image tag to pull from GHCR (use a version like v0.2.0 or latest)
# MINEOS_IMAGE_TAG=latest

# Default shutdown timeout (seconds) for stopping Minecraft servers
# MINEOS_SHUTDOWN_TIMEOUT=300

# ============================================
# Optional: External Integrations
# ============================================
# CurseForge API key for mod/modpack downloads
# Get yours at: https://console.curseforge.com/
# CurseForge__ApiKey=

# Discord webhook URL for notifications
# Discord__WebhookUrl=https://discord.com/api/webhooks/...

# ============================================
# Application Ports
# ============================================
API_PORT=5078
# Public HTTP port for the UI proxy
WEB_PORT=3000

# Minecraft Java port range exposed on the host (TCP/UDP)
MC_PORT_RANGE=25565-25570

# Minecraft Bedrock port range exposed on the host (UDP only)
BEDROCK_PORT_RANGE=19132-19137

# ============================================
# Web UI Configuration
# ============================================
# ORIGIN is OPTIONAL. MineOS accepts any address that reaches this machine
# (localhost, LAN IP, DNS name) — login works from all of them automatically.
# Set this only to pin a canonical origin (also accepted in addition to the
# request's own Host), e.g. behind an unusual reverse proxy setup.
# Examples: http://myserver:3000, https://mineos.example.com
ORIGIN=http://localhost:3000

# Public web origin for CORS (usually same as ORIGIN)
# WEB_ORIGIN_PROD=http://localhost:3000

# Browser-facing API base URL (leave empty to use browser's current URL - recommended)
# PUBLIC_API_BASE_URL=

# Dev UI origin for CORS/CSRF in dev container
# WEB_ORIGIN_DEV=http://localhost:5174

# Allow additional hosts for Vite dev server (comma-separated)
# VITE_ALLOWED_HOSTS=dev-server-1

# Git identity for dev container commits (optional)
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=you@example.com
# GIT_DEFAULT_BRANCH=main
# DEV_INSTALL_GIT=true

# ============================================
# Minecraft Server Address
# ============================================
# Public hostname or IP used for the "Copy Address" button in the UI
# PUBLIC_MINECRAFT_HOST=mc.example.com
#
# Optional: expose the configured port range to the UI
# PUBLIC_MC_PORT_RANGE=25565-25570

# ============================================
# Web UI Upload Limits
# ============================================
# Maximum request body size for the web UI (SvelteKit).
# Use Infinity to disable the limit.
# BODY_SIZE_LIMIT=Infinity

# ============================================
# Telemetry (anonymous usage data)
# ============================================
# MINEOS_TELEMETRY_ENABLED=true
# MINEOS_TELEMETRY_ENDPOINT=https://mineos.net
# MINEOS_TELEMETRY_KEY=
# MINEOS_INSTALLATION_ID=

# ============================================
# Logging Configuration
# ============================================
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning

# ============================================
# Advanced Settings (optional)
# ============================================
# ASPNETCORE_ENVIRONMENT=Production
# ASPNETCORE_URLS=http://+:5078

# ============================================
# HTTPS (Optional)
# ============================================
# For HTTPS, use a reverse proxy like nginx, Traefik, or Caddy
# in front of the web container (port 3000)
