# mineos.conf but non-tracked, kept intact through pulls
custom.conf
mineos.auth.log

# Logs
logs
*.log
# ...but not source directories that happen to be called "logs". The bare
# pattern above matches at any depth, which silently excluded the proxy Logs
# route: it worked locally and was missing for everyone else.
!apps/web/src/routes/**/logs/
!apps/web/src/routes/**/logs/**

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# https://docs.npmjs.com/misc/faq#should-i-check-my-node-modules-folder-into-git
node_modules

# .NET build artifacts
bin
obj

# SQLite
*.db
*.db-shm
*.db-wal
apps/MineOS.Api/data/

# Local Minecraft data
/minecraft/

# Claude settings
.claude/settings.local.json
.claude/settings.json

# Environment files (keep .env.example)
.env
.env.local
.env.*.local
apps/web/.env
!.env.example
!apps/web/.env.example
tools/mineos-cli/mineos.exe
tools/mineos-cli/.idea/*
tools/mineos-cli/mineos.exe.old
mineos/*
.worktrees/

# Session handoff notes (local scratch, never for the public repo)
*-HANDOFF.md
